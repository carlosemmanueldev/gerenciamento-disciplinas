<%= form_with(model: classroom, class: "d-flex flex-column align-items-center") do |form| %>
  <% if classroom.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
        <% classroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-3">
    <%= form.label :school_year, "Série", style: "display: block" %>
    <%= form.text_field :school_year, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :letter, "Letra da turma", style: "display: block" %>
    <%= form.text_field :letter, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :year, "Ano", style: "display: block" %>
    <%= form.text_field :year, class: 'form-control' %>
  </div>

  <div>
    <%= form.submit id:"submit", class: "btn btn-orange" %>
  </div>
<% end %>
